[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -ra --strict-markers
markers =
    integration: mocked integration tests that exercise Flask routes and service boundaries
